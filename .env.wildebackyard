# WildeBackyard API Environment Variables
# This file is deployed with the GCP app

# Database (configured via app.yaml env_variables)
# WILDEBACKYARD_API_DATABASE_URL is set in app.yaml

# Django Secret Key (should move to Secret Manager in production)
DJANGO_SECRET_KEY=temp-insecure-key-change-to-secret-manager-value-asap

# Django Settings
DEBUG=False
ALLOWED_HOSTS=*

# GCP Configuration
GAE_APPLICATION=wildepod-339517
